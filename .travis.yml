# R for travis: see documentation at https://docs.travis-ci.com/user/languages/r

language: R

cache: packages
cache:
  directories:
  - $HOME/.cmdstanr

env:
  - _R_CHECK_FORCE_SUGGESTS_=false

before_install:
  - inst/make_cmdstan.sh

before_script:
  - export CMDSTAN='~/.cmdstanr/cmdstan'

# Everything compiled beside a test model is a build product: the executable,
# which has no extension, the C++ it was generated from, and the build record
# cmdstanr writes next to it. Ignore all of it and keep only the sources.
*
!.gitignore
!*.stan
